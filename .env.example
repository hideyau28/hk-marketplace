# Server-side (single-tenant instance)
# Each deployment serves ONE merchant with its own DB + ADMIN_SECRET.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require
ADMIN_SECRET=change-me

# Optional: Basic Auth (if unset, defaults to user=admin pass=ADMIN_SECRET)
ADMIN_BASIC_USER=admin
ADMIN_BASIC_PASS=change-me

# Stripe (optional)
# If unset, checkout + webhook routes will return 500 with a clear message.
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
# For local dev, this is printed by: `npm run stripe:listen`
STRIPE_WEBHOOK_SECRET=whsec_...

# Cloudinary (optional - for product image uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Analytics (optional)
# Google Analytics 4 Measurement ID
NEXT_PUBLIC_GA_ID=
# Meta Pixel ID
NEXT_PUBLIC_META_PIXEL_ID=

# Google OAuth (optional - for tenant admin Google sign-in)
# Create credentials at https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Tenant Admin JWT secret (required for tenant admin auth)
TENANT_JWT_SECRET=change-me

# Base URL (recommended)
# Used to build Stripe success/cancel URLs and OAuth redirect URIs.
# In prod, set to your canonical HTTPS origin.
APP_URL=http://localhost:3012

# Client-side (admin UI secret is stored in sessionStorage at runtime; no NEXT_PUBLIC secret needed)
# Do NOT set NEXT_PUBLIC_ADMIN_SECRET (never expose ADMIN_SECRET client-side).
